<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:card="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:layout_marginTop="@dimen/list_item_margin"
    android:layout_marginLeft="@dimen/list_item_margin"
    android:layout_marginRight="@dimen/list_item_margin"
    card:cardCornerRadius="8dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/card_view_vertical_padding"
        android:paddingBottom="@dimen/card_view_vertical_padding"
        android:paddingLeft="@dimen/card_view_horizontal_padding">

        <TextView
            android:id="@+id/password_ssid"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textAppearance="@android:style/TextAppearance.Medium"
            tools:text="SSID"/>

        <TextView
            android:id="@+id/password_key"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/password_ssid"
            android:textAppearance="@android:style/TextAppearance.Small"
            tools:text="password"/>

    </RelativeLayout>

</android.support.v7.widget.CardView>